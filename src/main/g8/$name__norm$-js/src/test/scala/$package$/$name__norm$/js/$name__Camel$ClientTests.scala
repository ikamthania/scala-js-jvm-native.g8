package $package$.$name$.js

import org.scalatest.funsuite.AnyFunSuite

object $name;format="Camel"$ClientTests extends AnyFunSuite {

  test("An empty Set should have size 0") {
    assert(Set.empty.size == 0)
  }

}
